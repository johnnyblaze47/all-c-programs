#include<stdio.h>//which value is greater//
void main()
{
	int x, y;
	printf("\nEnter value for x");
	scanf("%d", &x);
	printf("\nEnter value for y");
	scanf("%d", &y);
	if (x >= y)
	{
		printf("\nx is greater than y");
	}
	else 
	printf("\ny is greater than x");
}
